@model IEnumerable<ServiceAppDemo.Models.Relevancia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescripRel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescripRel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodRel }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodRel }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodRel })
        </td>
    </tr>
}

</table>
