@model IEnumerable<ServiceAppDemo.Models.Tipo_Caso>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescripTip)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescripTip)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodTip }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodTip }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodTip })
        </td>
    </tr>
}

</table>
